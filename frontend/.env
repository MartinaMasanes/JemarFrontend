# URL base del backend (se hornea en el build de Vite).
# Local (perfil http de dotnet run):
VITE_API_URL=http://localhost:5186
# Alternativa perfil https: https://localhost:7098 (requiere dotnet dev-certs https --trust)
